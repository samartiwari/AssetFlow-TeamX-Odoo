import PagePlaceholder from "../components/PagePlaceholder";

export default function Dashboard() {
  return (
    <PagePlaceholder
      title="Dashboard"
      note="KPI cards, overdue returns, quick actions, and recent activity go here."
    />
  );
}
