import PagePlaceholder from "../components/PagePlaceholder";

export default function AllocationTransfer() {
  return (
    <PagePlaceholder
      title="Allocation & Transfer"
      note="Allocate assets, handle the double-allocation block, and run transfers here."
    />
  );
}
