import PagePlaceholder from "../components/PagePlaceholder";

export default function Notifications() {
  return (
    <PagePlaceholder
      title="Notifications"
      note="Notification feed and the full activity log go here."
    />
  );
}
