import PagePlaceholder from "../components/PagePlaceholder";

export default function Assets() {
  return (
    <PagePlaceholder
      title="Assets"
      note="Asset registration, search, and the directory table go here."
    />
  );
}
